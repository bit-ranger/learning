# Which compiler
CC = gcc
# Where to install
INSTDIR = /usr/local/bin
# Where are include files kept
INCLUDE = .
# Options for development
CFLAGS = -g -Wall -ansi

# Options for release
# CFLAGS = -O -Wall –ansi
# Local Libraries
THREAD_LIB = threadlib.a

popen: popen.c
	$(CC) popen.c -o popen




